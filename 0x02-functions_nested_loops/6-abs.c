#include "main.h"

/**
 * _abs - computes the absolute value
 * @n: the int to check
 * Return: the absolute value of an integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
