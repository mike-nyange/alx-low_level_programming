#include "main.h"

/**
 * _strlen - a function that returns the length of a string.
 * @s: string whose length we are finding
 *
 * Return: the length of the string.
 */

int _strlen(char *s)
{
	return strlen(*s);
}
