#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char first;


	for (first = 'a'; first <= 'z'; first++)
	{
		putchar(first);
	}
	putchar('\n');
	return (0);
}
