#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: digit to be multiplied to b
 * @b: digit to be multiplied to a
 * Return: product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
